# lvsea-writing 中文使用说明

## 触发词

可以直接说：

- 使用 $lvsea-writing 把资料、结构、读者、风格、场景、初稿、最后去 AI 味和人工终审串成一条线，不要一上来就润色。
- 使用 $lvsea-writing 先研究和搭主线，等完整初稿出来后最后一步再去 AI 味；没有证据的地方标为待核验。
- 使用 $lvsea-writing 先把研究、提纲和技术核验跑完，再写教程，最后做 Humanizer 终稿审校。
- 只使用 `$lvsea-writing` 作为入口：如果任务从小红书素材开始，先路由到 `$lvsea-xiezuo`，写成完整成稿后再进入 `$lieflat-less-ai-tone` 的最后编辑阶段。

- 使用 `$lvsea-writing` 去掉这篇文字的 AI 味，但保留事实和我的判断。
- 用 `$lvsea-writing` 按制造企业管理者的语气重写这份材料。
- 先用 `$lvsea-writing` 检测这篇稿子的 AI 痕迹，再改写，不要先动原文。
- 用 `$lvsea-writing` 学我的写作样本，但不要把我的口头禅机械塞进每一段。
- 用 `$lvsea-writing` 给这篇稿子做 AI 痕迹评分，并告诉我哪些是高置信问题、哪些只是人工判断项。
- 用 `$lvsea-writing` 按深度长文流程，从选题、证据、提纲、初稿、审稿到事实核查逐步推进。

## 推荐主线

### 单入口路由

先判断材料状态和交付目标：

```text
小红书素材/指标/评论 -> $lvsea-xiezuo -> source-backed handoff
普通写作/改稿/研究 -> $lvsea-writing 主流程
完整成稿/只去 AI 味 -> $lieflat-less-ai-tone 或严格后处理降级模式
组合任务 -> xiezuo -> writing -> lieflat -> 作者人工终审
```

不要把终稿后处理器提前用于资料、提纲或未完成初稿，也不要为了一个普通改稿任务启动素材专家。

默认不是“把所有提示词一次性塞给模型”，而是按任务规模串联阶段：

~~~text
任务契约 -> 资料/证据 -> 主判断/提纲 -> 读者检查 -> 声音校准
-> 场景/格式 -> 初稿/编辑 -> 最后去 AI 味 -> 作者人工终审
~~~

短改稿可以合并前几步；研究型长文、教程和正式材料不要跳过资料与结构。去 AI 味只能处于最后编辑层，不能用来补事实、补经历、补主线或替代技术核验。中间产物和每个阶段的通过门见 [pipeline.md](references/pipeline.md)。

## 推荐输入

尽量给出原文或材料、使用场景、目标读者、希望保留的事实和语气。没有补充时，Skill 会采用中文、事实优先、结论清楚、表达克制的默认模式。

需要学习风格时，最好提供 3–5 篇同类、近期、确认由同一作者写的样本；需要深度写作时，补充来源、截止时间、篇幅和是否需要中间文件。

个人经历、政策事实、产品能力、价格、版本、数字和引语缺少来源时，不会自动补写。可以允许检索，也可以明确哪些部分只是推断或虚构。

## 四个真实应用场景

### 1. 制造管理与生产运营

```text
使用 $lvsea-writing，把这份生产异常记录改成给管理层看的内部通报。先保留日期、产线、产品、损失和责任边界，再把原因、措施和待确认项写清楚。不要编造数据，不要写营销腔。
```

默认保留工艺、设备、质量、安全和产能术语，重点让责任、风险、行动和条件读起来清楚。

串联方式：先核对异常材料和责任边界，再搭“发生了什么—影响—原因—措施—待确认”结构，完成正式通报后才做克制的去 AI 味和人工终审。

### 2. 安全管理、党建党政与政策解读

```text
使用 $lvsea-writing，把这份政策材料整理成一篇庄重、准确、适合企业内部学习的说明。不要娱乐化，不要替原文扩大政治结论，保留正式表述，并把对企业实际工作的影响写具体。
```

默认优先核实来源和时效，不为了“像真人”加入随意口吻、虚构经历或未经授权的心理描写。

串联方式：先过来源、发布时间和适用范围，再组织正式结构和企业行动，最后只清理模板腔，不改政治表述和事实边界。

### 3. 公众号、知乎与行业解读

```text
使用 $lvsea-writing，把下面的行业材料改成一篇公众号文章。先确认材料能支撑的结论，再删掉重复解释和模板小标题。开头尽快进入事实，保留数字、案例和限制，写得自然但不要故意装口语。
```

可启用温暖表达和具体过程，但生活故事、心理描写和个人经历必须来自材料或明确的虚构授权。

串联方式：先研究材料并锁定可被反驳的判断，再搭文章主线、做读者检查和声音校准，初稿完成后最后一步才去 AI 味。

### 4. 视频口播与 PPT 页面文字

```text
使用 $lvsea-writing，把这份解读稿改成视频旁白和 PPT 页面文字。旁白要能朗读，句子长短有变化；PPT 文字保留事实、数字和行动要求。不要把旁白写成论文，也不要为了去 AI 味删除必要结构。
```

默认把旁白和页面文字分开处理，不把“来源说明”“AI 生成说明”等制作备注塞进画面正文，除非用户明确要求。

串联方式：先核对素材和行动要求，再分别搭旁白与页面结构，完成朗读/版式检查后才做去 AI 味和人工终审。

## 模式

- **默认改稿**：保真重写，直接返回成稿。
- **创作**：从用户材料出发，先过材料门，再组织内容。
- **检测后改写**：先列意义膨胀、宣传腔、模糊归因、公式句、AI 高频词和风格问题，再改写。
- **统计检测**：输出模式命中、句长波动、词汇重复、Unicode 残留和结构风险；不输出“这一定是 AI 写的”这种结论。
- **深度长文**：按证据账本、立场、提纲、写作、审稿、去痕、事实核查和终稿的顺序推进，只有用户需要时才落盘中间文件。
- **流水线长文**：按“资料→结构→读者→声音→场景→初稿→最后去 AI 味→人工终审”推进；任何前置阶段未通过，都不靠润色掩盖。
- **素材接力**：小红书素材任务先由 `$lvsea-xiezuo` 形成证据卡和写作接力包，再回到 `$lvsea-writing`；最终完整成稿才进入 `$lieflat-less-ai-tone`。
- **已有成稿终审**：只在事实、结构和场景已稳定时进入严格白名单式后处理，不做结构重写。
- **温暖质感**：仅在用户要求“亲切一点、像朋友聊天、像本人写的”时启用。
- **风格学习**：最好提供三到五篇同类、近期、本人写的样本。样本不会自动写入公开仓库。

## 检查脚本

```powershell
python scripts/check_writing.py path\to\draft.md --scenario general
python scripts/check_writing.py path\to\draft.md --scenario professional
python scripts/check_writing.py path\to\draft.md --scenario social --texture
python scripts/check_writing.py path\to\draft.md --json
```

脚本只做确定性检查和风险提醒，不自动修改文章，也不能代替事实核验、朗读、独立读者测试和人工判断。Windows 默认编码环境下运行脚本时，必要时先设置 `$env:PYTHONUTF8='1'`。

## 注意事项、限制与能力边界

- 去 AI 味不等于规避检测，也不保证任何平台的检测结果。
- 任何统计分数都只是当前文本的风险信号，不能证明作者身份、写作来源或平台会如何判定。
- Skill 不会凭空创造事实、来源、用户经历、数字、引语或产品能力。
- “赋能”“闭环”“生态”“底层逻辑”等词不是全部禁用。真实描述机制、流程或行业对象时可以保留，否则优先换成具体动作。
- 正式报告、党政材料、技术文档和 PPT 需要 Markdown、表格、编号或术语时，不强行改成聊天体。
- “嗯”“其实吧”“说实话”、省略号和轻微不完整句不是默认装饰，只有在场景和样本支持时少量使用。
- 学习模式只提取样本中有证据的稳定特征，不复制原句，不把偶然特征当成长期风格。
- 医疗、法律、金融、政策和安全内容仍需专业来源或人工复核，Skill 不能替代专业判断。
- 长文工作流可以生成证据账本、版本说明、事实核查报告和终稿，但这些文件不能替代用户对来源和结论的最终确认。
- 三个 Skill 不要求同时安装：主入口可以在专家缺失时降级；缺失 provider、素材证据或人工终审时必须明确写出缺口。
- “人工终审”不是模型自称完成的标签。没有作者或真实人工确认时，只能交付“编辑完成、终审待确认”，不能宣称已经完全像本人或可以直接署名。
