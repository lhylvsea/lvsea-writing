interface:
  display_name: "Lvsea Writing"
  short_description: "面向制造管理与专业内容的中文事实写作、自然表达与去AI味终稿助手"
  default_prompt: "使用 $lvsea-writing，把这份材料写得自然、具体、可信，并按场景保留必要结构。"

policy:
  allow_implicit_invocation: true
